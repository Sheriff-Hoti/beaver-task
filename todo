next fuse the background and foreground stuff to manager because the manager must be responsible for list and keypairs and the backround only for the 	return appStyle.Render(m.list.View())
build list with lipgloss styles